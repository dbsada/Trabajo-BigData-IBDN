apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask
  namespace: ibdn
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      serviceAccountName: flask-sa
      containers:
      - name: flask
        image: europe-west1-docker.pkg.dev/REPLACE_PROJECT/ibdn/flask:latest
        ports:
        - containerPort: 5001
        volumeMounts:
        - name: flask-code-patch
          mountPath: /app/scripts/web/predict_flask.py
          subPath: predict_flask.py
        envFrom:
        - configMapRef:
            name: ibdn-config
        env:
        - name: FLASK_DEBUG
          value: "1"
        - name: PROJECT_HOME
          value: "/app/"
        - name: SKIP_AUTO_START_PREDICTION
          value: "1"
        - name: DEPLOY_MODE
          value: "gke"
        - name: FLASK_PORT
          value: "5001"
        resources:
          requests:
            memory: "256Mi"
          limits:
            memory: "512Mi"
      volumes:
      - name: flask-code-patch
        configMap:
          name: flask-code-patch
---
apiVersion: v1
kind: Service
metadata:
  name: flask
  namespace: ibdn
spec:
  selector:
    app: flask
  ports:
  - port: 5001
